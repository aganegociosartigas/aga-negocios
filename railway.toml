[build]
builder = "nixpacks"
buildCommand = "prisma generate && next build"

[deploy]
startCommand = "prisma db push --accept-data-loss && next start -p $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
